# Set the base path for the API
server:
  port: 13001
  servlet:
    context-path: /api/weather

management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      probes:
        enabled: true

spring:
  cache:
    type: redis
  data:
    redis:
      host: weather-redis
      port: 6379
  datasource:
    url: jdbc:postgresql://weather-postgres:5432/weather_service?useUnicode=true&characterEncoding=UTF-8
    username: weather_service
    password: ${POSTGRES_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        connection:
          useUnicode: true
          characterEncoding: UTF-8
  jackson:
    default-property-inclusion: non_null
    deserialization:
      fail-on-unknown-properties: false

security:
  excluded-paths: /actuator/health,/actuator/info,/actuator/prometheus,/api/weather/actuator/health,/api/weather/actuator/info,/api/weather/actuator/prometheus,/swagger-ui.html,/swagger-ui/**,/*/api-docs

authentication:
  cors:
    allowed-origin-patterns: http://localhost:3030,https://weather.markpost.dev

logging:
  level:
    root: DEBUG
    nl.markpost.weather: DEBUG

jwt:
  public-key-url: https://auth.markpost.dev/api/auth/v1/public-key

---
# Local environment profile
spring:
  config:
    activate:
      on-profile: local
  cache:
    type: redis
  data:
    redis:
      host: localhost
      port: 13004
  datasource:
    url: jdbc:postgresql://localhost:13003/weather_service
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  jackson:
    default-property-inclusion: non_null
    deserialization:
      fail-on-unknown-properties: false

authentication:
  cors:
    allowed-origin-patterns: http://localhost:3030

jwt:
  public-key-url: http://localhost:3000/api/auth/v1/public-key

---
# UT environment profile
spring:
  config:
    activate:
      on-profile: ut
  datasource:
    url: jdbc:h2:mem:testdb
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    database-platform: org.hibernate.dialect.H2Dialect

security:
  excluded-paths: /actuator/health,/actuator/info,/swagger-ui.html,/swagger-ui/**,/*/api-docs,/v1/forecast,/v1/saved-locations
